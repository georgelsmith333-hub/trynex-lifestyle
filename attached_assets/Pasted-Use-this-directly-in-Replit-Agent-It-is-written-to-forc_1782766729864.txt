Use this **directly in Replit Agent**. It is written to force a real full-stack audit first, then fix safely without breaking your TryNex shop.  You are a senior full-stack production engineer. I need you to audit and fix my TryNex shop fully, carefully, and safely.  IMPORTANT CONTEXT: My TryNex shop was working before, but now it is not working properly. It is not showing real-time/live data. I suspect the Neon PostgreSQL database, API routing, backend/frontend connection, environment variables, CORS, Prisma/ORM, Render/Replit deployment config, or some earlier AI/Replit agent mistake broke the connection. I also need the Design Studio / Customize page fixed for desktop and mobile scrolling, layout, responsiveness, product preview, and customization UI.  MAIN GOAL: Do a complete A-to-Z production-level audit and fix the full stack without breaking anything. Make the shop fully connected, live, responsive, stable, and production-ready.  STRICT RULES:  1. Do NOT rewrite the whole project unnecessarily. 2. Do NOT delete important files, database tables, migrations, images, products, orders, or user data. 3. Do NOT change the main business logic unless it is broken. 4. Do NOT hardcode fake/mock data if real database/API data should be used. 5. Do NOT leave temporary test code, console spam, broken comments, or unused files. 6. Before making changes, inspect the current architecture and identify the real cause. 7. Make small, safe, professional fixes. 8. After each fix, run tests/build/typecheck/lint where available. 9. Keep design premium and responsive. 10. If any env variable is missing, clearly list the exact variable name and where it must be added.  PHASE 1 — FULL PROJECT AUDIT: First inspect the whole project structure:  * frontend framework and routes * backend/API structure * database connection setup * Neon/PostgreSQL/Prisma/ORM config * environment variables * API base URL usage * real-time/live data fetching * product/order/customization data flow * image storage / Cloudflare R2 or other storage usage * deployment config for Replit/Render/Cloudflare Pages if present * build scripts and package scripts  Find exactly why live data is not showing. Check:  * DATABASE_URL / Neon connection * API endpoint URLs * frontend API client base URL * CORS settings * server port/listener setup * API route prefixes * Prisma/schema/migrations/generate status * broken imports * broken fetch/axios calls * wrong localhost URLs in production * stale mock data replacing real API data * failed hooks/state management * auth/session/localStorage issues * errors in browser console and server logs * missing deployed backend URL * mismatched table/model names * wrong environment variable naming such as VITE_API_URL, REACT_APP_API_URL, NEXT_PUBLIC_API_URL, DATABASE_URL, DIRECT_URL, etc.  PHASE 2 — DATABASE + API FIX: Fix the Neon database connection properly.  * Ensure backend connects to Neon PostgreSQL using secure env variables. * Ensure ORM/prisma client is generated correctly. * Ensure migrations/schema are valid. * Ensure API routes return real database data. * Ensure product, order, design/customization, user/cart/checkout data are connected correctly. * Add or fix health-check endpoint if missing, such as /api/health. * Add safe error handling so frontend shows useful messages instead of blank pages. * Never expose DATABASE_URL to the frontend.  PHASE 3 — FRONTEND LIVE DATA FIX: Fix the frontend so it always calls the correct backend/API.  * Remove wrong localhost-only URLs from production code. * Centralize API base URL in one config/helper file. * Ensure frontend uses real API responses, not stale hardcoded/mock data. * Add loading, empty, and error states. * Fix hydration/state issues if applicable. * Make sure product data, customization data, cart, checkout, and order status update correctly.  PHASE 4 — DESIGN STUDIO / CUSTOMIZE PAGE FIX: Deeply audit and fix the Design Studio / Customize page for both desktop and mobile. Focus on:  * scrolling problems * page overflow problems * sticky/fixed panels causing hidden content * canvas/mockup preview sizing * product preview not fitting screen * mobile responsiveness * desktop layout spacing * bottom buttons not visible * toolbar/options panel overlap * image upload/custom text controls * color selection * product variant selection * add-to-cart flow from customized product * duplicate images or wrong preview rendering * long sleeve/hoodie/t-shirt mockup mismatch if present * white/black product color preview bugs * design area not clipping or scaling correctly  Make it premium:  * clean responsive layout * smooth mobile usability * no horizontal scroll unless intentionally needed * clear buttons * visible save/add-to-cart actions * proper spacing * no broken overlapping sections * professional e-commerce UI feel  PHASE 5 — RESPONSIVE SITE-WIDE UI AUDIT: Check the whole shop on:  * mobile width around 360px–430px * tablet width around 768px * desktop width around 1366px