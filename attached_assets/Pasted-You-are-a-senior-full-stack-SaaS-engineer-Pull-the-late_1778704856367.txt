You are a senior full-stack SaaS engineer. Pull the latest code from:
https://github.com/georgelsmith333-hub/trynex-liestyle

FIRST PRIORITY: PRODUCTION WEBSITE IS STILL UNSTABLE.

Live symptoms:
- https://trynex-api.onrender.com/api/healthz returns OK
- https://trynexshop.com may show 403 / blocked / not loading
- https://www.trynexshop.com may show 502
- navigation sometimes shows white blank screen between pages
- first load can be slow
- page-to-page transitions feel broken
- frontend deployment checks may have Cloudflare Pages/Workers conflicts

DO NOT start by adding features.

PHASE A — DEPLOYMENT + DOMAIN FIX

Audit and fix:
1. Cloudflare DNS
2. Cloudflare Pages project
3. Cloudflare Workers conflict
4. www → root redirect
5. root → correct Pages deployment
6. _redirects
7. _headers
8. SPA fallback routing
9. Cloudflare cache rules
10. GitHub Actions workflows
11. duplicate Cloudflare projects
12. Workers Builds failing
13. Pages deploy workflow failing
14. Render backend deploy health
15. frontend API_BASE_URL correctness

Goal:
- https://trynexshop.com loads normally
- https://www.trynexshop.com redirects or loads normally
- no 403
- no 502
- no blank white screen
- GitHub checks pass cleanly

PHASE B — WHITE SCREEN / ROUTING FIX

Deeply inspect React/Vite routing:

Check:
- App.tsx
- lazy imports
- Suspense
- AnimatePresence
- ErrorBoundary
- service worker
- cache-recovery logic
- chunk load failures
- route transitions
- hydration errors
- browser console errors
- mobile navigation
- React Query request behavior

Fix:
- white screen between pages
- blank screen on route click
- chunk loading crashes
- bad loading animation
- layout flash
- reload-required navigation
- missing route-level error boundaries
- bad fallback loaders

Add:
- route-level skeleton loaders
- chunk load retry recovery
- clean transition loader
- graceful offline/API-fail state
- better global error UI

PHASE C — NEON LIMIT / MULTI-DB SAFETY

I have multiple Neon DBs available, but DO NOT randomly shard everything.

First audit current DB usage:
- most-used tables
- largest tables
- highest query endpoints
- transfer-heavy endpoints
- repeated reads
- polling endpoints
- missing indexes
- cache misses

Use this DB strategy safely:

MAIN DB:
- users
- auth
- sessions
- orders
- products
- settings

PRODUCTS DB / optional:
- product catalog only if current structure supports safe separation

ANALYTICS DB:
- logs
- events
- tracking
- non-critical analytics

FAILOVER DB:
- emergency backup only

NEVER split transactional data if it causes conflict.
NEVER split orders/users across DBs unless there is a clean routing layer.
NEVER create data inconsistency just to avoid Neon limits.

Instead prioritize:
- Redis cache
- pagination
- indexes
- smaller payloads
- no SELECT *
- no nonstop polling
- no repeated API calls
- no full DB reads on every page

PHASE D — CACHE + REDIS HARDENING

Verify:
- Redis is connected
- MISS→HIT works
- cache keys are namespaced
- cache invalidation works
- products/categories/settings/blog update instantly after admin changes
- no stale storefront bugs
- Redis failure does not break app

Fix:
- stale cache issues
- cache key collisions
- unbounded search cache
- missing invalidation
- no X-Cache headers
- deployment stale cache issues

PHASE E — ADMIN PANEL

Audit and fix:
- admin login
- dashboard
- products
- categories
- orders
- blog
- settings
- mobile admin layout
- table overflow
- mutation failures
- stale admin data
- admin error boundaries

PHASE F — FINAL VERIFICATION

Run and report:
1. git status
2. TypeScript checks
3. production build
4. API health tests
5. frontend route tests
6. admin route tests
7. Redis tests
8. DB connection tests
9. Cloudflare deployment checks
10. Render health check
11. mobile responsive check
12. browser console error check

Required final report:
- exact root cause of 403/502
- exact root cause of white screen
- files changed
- tests run
- deployment result
- remaining risks
- what still needs manual setup
- whether multi-Neon is actually needed now

IMPORTANT:
Fix production stability first. Do not over-engineer. Do not break current working backend. Do not expose secrets in files or reports.