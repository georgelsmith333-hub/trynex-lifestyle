Send this to the new Replit agent so it understands the FULL situation before touching anything:

READ THIS FULLY BEFORE TOUCHING THE PROJECT.

This project already went through emergency recovery, cache optimization, deployment stabilization, Redis integration, admin routing fixes, and GitHub workflow repairs.

Current architecture:

* Frontend:
  Cloudflare Pages
  Main project:
  trynex-lifestyle-shop

* API:
  Render
  trynex-api.onrender.com

* Primary Database:
  Replit PostgreSQL ONLY

* Backup / failover only:
  Neon databases

* Cache:
  Upstash Redis

* Storage:
  Cloudflare R2

* Source control:
  GitHub repo:
  georgelsmith333-hub/trynex-liestyle

DO NOT:

* rebuild architecture
* shard production DB
* create duplicate APIs
* create duplicate Workers
* migrate away from current primary DB
* remove cache systems
* remove service worker
* break deployment pipelines

FIRST TASK:
Understand what already exists before coding.

YOU MUST:

1. Read all current GitHub workflows.
2. Read render.yaml.
3. Read wrangler.toml.
4. Read vite.config.ts.
5. Read App.tsx routing structure.
6. Read Redis helper implementation.
7. Read admin routing system.
8. Read deployment PDF reports already generated.
9. Verify current deployment health.
10. Verify admin routes directly.

Then produce:

* architecture summary
* deployment summary
* active services summary
* cache topology summary
* DB topology summary
* unresolved risks summary

THEN verify:

* admin/orders works
* direct route refresh works
* no white screen
* no hydration mismatch
* no stale SW cache
* no deployment race
* Redis MISS→HIT works
* Render health endpoint works
* Cloudflare Pages latest deploy works

THEN continue ONLY with:

* production hardening
* observability
* UX polish
* deployment normalization
* monitoring
* security hardening
* documentation

Required final deliverables:

* updated production audit PDF
* deployment architecture PDF
* admin verification PDF
* buyer flow verification PDF
* cache verification PDF
* infrastructure readiness report

Every report must contain:

* timestamps
* screenshots
* route maps
* deployment maps
* environment summaries
* readiness score
* remaining risks
* recommendations

Before any major changes:
ALWAYS backup current branch first.
