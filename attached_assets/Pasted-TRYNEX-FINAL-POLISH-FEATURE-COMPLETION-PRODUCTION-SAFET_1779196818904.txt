TRYNEX FINAL POLISH + FEATURE COMPLETION + PRODUCTION SAFETY TASK

You are the senior full-stack production engineer for TryNex Lifestyle.

IMPORTANT CURRENT STATUS:
The emergency phase is over. The latest Cloudflare Pages deployment succeeded after fixing garment3d merge conflicts. Do NOT rebuild the whole system. Do NOT rewrite architecture. Do NOT break working production.

CURRENT KNOWN STACK:
- Frontend/Admin: Cloudflare Pages project trynex-lifestyle-shop
- Source: GitHub georgelsmith333-hub/trynex-liestyle
- API: Render trynex-api.onrender.com
- DB: currently documented as Replit PostgreSQL primary, Neon DBs as backup/failover
- Cache: Upstash Redis
- Storage: Cloudflare R2
- CDN/DNS: Cloudflare
- Frontend: React/Vite/Wouter/Tailwind/Three.js
- Backend: Express/Drizzle/PostgreSQL

FIRST RULES:
1. Do not expose secrets.
2. Do not commit .env or secret CSV.
3. Do not change DATABASE_URL blindly.
4. Do not shard DB randomly.
5. Do not create new Workers.
6. Do not rewrite Design Studio from scratch.
7. Fix incrementally, verify, then continue.
8. Always run build before pushing.
9. Generate professional PDF report before final checkpoint.

==================================================
PHASE 1 — PRODUCTION HEALTH + CLEANUP
==================================================

Check current live status:
- https://trynexshop.com
- https://www.trynexshop.com
- https://trynexshop.com/admin
- https://trynex-api.onrender.com/api/healthz

Fix remaining Cloudflare warning:
Cloudflare log showed invalid redirect:
"/* /index.html 200" infinite loop.
Find and fix _redirects / wrangler / Pages routing safely.
Use proper SPA fallback only:
- Cloudflare Pages should serve SPA routes correctly.
- No infinite redirect warning.
- Direct refresh must work on:
  /products
  /cart
  /checkout
  /admin
  /admin/orders
  /design-studio

Do not break existing deployed app.

==================================================
PHASE 2 — DATABASE TOPOLOGY CLARITY
==================================================

Audit and document exactly:
- Which DB is ACTIVE primary
- Which Neon DBs are failover only
- Whether any Products/Analytics DB is actually used
- Whether failover chain is safe
- Whether any DB has unique data
- Whether no unsafe sharding exists

Do NOT implement live sharding unless already designed and safe.
If multi-Neon splitting is not complete, document clearly:
- current state
- recommended future plan
- risk
- safe migration path

Create admin-visible DB status if already supported, but do not break DB.

==================================================
PHASE 3 — DESIGN STUDIO SELECTION UX FIX
==================================================

Current issue:
When user uploads or AI-generates an image/design and clicks it once, it does NOT clearly show selected state, resize handles, corner handles, rotation/position controls. User has to drag first before controls appear, which is confusing.

Fix Design Studio carefully:
1. Single click/tap on any design element must select it.
2. Selected image must immediately show:
   - bounding box
   - corner handles
   - side handles
   - rotation handle if supported
   - clear active state
3. Works on desktop and mobile touch.
4. Corner resize handles must allow diagonal resizing like Canva.
5. Side handles must allow horizontal/vertical resizing.
6. Dragging should still work.
7. Tap outside should deselect.
8. No accidental movement on selection click.
9. Selection controls should not be hidden behind mockup/canvas.
10. Preserve all current design studio functionality.
11. Do not rewrite the whole editor; patch the current interaction layer.

Also verify:
- uploaded image
- AI generated image
- logo/banner image
- mockup preview
- cart add
- checkout transfer

==================================================
PHASE 4 — CUSTOM ORDER ASSET HANDLING
==================================================

Current issue:
Admin custom orders show the final mockup image, but admin also needs the original uploaded/AI-created design asset in HD/full resolution.

Implement:
1. When customer customizes product, save BOTH:
   - final mockup preview image
   - original uploaded/generated artwork file in high resolution
2. Order record/admin order detail must show:
   - mockup preview
   - original artwork/download link
   - artwork filename/type
   - uploaded/generated source
3. Admin must be able to download original artwork.
4. Do not degrade image quality.
5. Store files in R2 when available, fallback safely if not.
6. Existing old orders should not break if original artwork missing.
7. Add graceful UI: “Original artwork unavailable” for old orders.

==================================================
PHASE 5 — CUSTOMER ↔ ADMIN MESSAGE SYSTEM
==================================================

Add a simple professional order communication feature.

Goal:
Admin can message customer about an order, and customer can reply.

Implement safely:
1. Admin order detail page:
   - “Send message to customer”
   - message text box
   - optional file/image attachment if easy and safe
   - send button
2. Customer side:
   - order tracking/account page shows messages for that order
   - customer can reply
   - optional attachment support if safe
3. Notification:
   - customer should see site notification/message status
   - email notification optional if SMTP configured
4. Backend:
   - create order_messages table if missing
   - link messages to orderId/customerEmail/customerId
   - sender type: admin/customer
   - read status
   - createdAt
   - attachmentUrl optional
5. Security:
   - customer can only access their own order messages
   - admin can access all
   - rate limit replies
   - validate uploads
6. Do not expose private messages publicly.

If full chat is too large for current usage, implement minimum viable version:
- admin sends order message
- customer sees/replies from track-order/account
- stored in DB
- no realtime required yet

==================================================
PHASE 6 — PRODUCT COLOR/STOCK AVAILABILITY
==================================================

Current problem:
Products show multiple colors/sizes/options, but admin needs control to mark specific color/variant out of stock or disabled.

Implement:
1. Admin product editor:
   - manage colors
   - mark color active/inactive
   - mark color in stock/out of stock
   - optionally stock quantity per color
2. Storefront:
   - unavailable colors should either be hidden or shown disabled
   - disabled color says “Out of stock”
   - customer cannot add unavailable color to cart
3. Checkout:
   - revalidate selected color/size before order placement
4. Backward compatible with existing products.
5. Do not break current color JSON structure; migrate safely if needed.
6. Add clear admin UI labels.

==================================================
PHASE 7 — ADMIN POWER POLISH
==================================================

Improve admin without huge rewrite:
- better order detail
- custom order assets panel
- color/stock controls
- deployment status visibility
- DB cluster visibility
- cache status visibility if existing
- graceful loading/error states
- mobile responsiveness

No admin route should show storefront 404.

==================================================
PHASE 8 — BUYER UX POLISH
==================================================

Verify and polish:
- mobile menu
- product page
- product detail
- design studio
- cart
- checkout
- track order
- account
- blog
- direct refresh
- no blank screen
- no white screen
- no stale service worker issue

==================================================
PHASE 9 — BUILD + DEPLOY SAFETY
==================================================

Before pushing:
Run:
git status
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm run build
grep -R "<<<<<<<\\|=======\\|>>>>>>>" -n artifacts lib packages .github || true

There must be:
- no merge conflict markers
- no secrets committed
- no build errors
- no type errors

Push safely to GitHub main only after successful build.

==================================================
PHASE 10 — REQUIRED FINAL PDF REPORT
==================================================

Before final checkpoint, generate a professional PDF report.

Name:
TRYNEX_FINAL_POLISH_AND_VERIFICATION_REPORT.pdf

Report must include:
1. Executive summary
2. What was fixed
3. Current live architecture
4. DB topology: active vs failover
5. Redis/cache status
6. Cloudflare Pages status
7. Render API status
8. Design Studio selection UX verification
9. Custom order asset handling verification
10. Customer/admin messaging status
11. Product color/stock controls status
12. Buyer route QA table
13. Admin route QA table
14. API health table
15. Build/typecheck result
16. Screenshots list
17. Remaining risks
18. Next recommended improvements
19. Production readiness score

PDF rules:
- mask all secrets
- no API tokens
- no DB URLs with passwords
- no admin password
- include timestamp
- include Git commit hash
- include deployment status

==================================================
FINAL SUCCESS CRITERIA
==================================================

Do not stop until:
- Cloudflare build passes
- no infinite redirect warning if fixable
- design click selection works
- resize/corner handles work
- original custom artwork saved/downloadable
- admin can message customer
- customer can reply or view message
- color stock controls work
- admin routes work
- buyer routes work
- build passes
- typecheck passes
- PDF report generated